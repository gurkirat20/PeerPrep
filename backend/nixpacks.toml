# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo 'No build step needed for backend'"]

[start]
cmd = "npm start"

